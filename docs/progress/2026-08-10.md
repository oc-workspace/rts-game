# 2026-08-10 Progress Log

## Scope

建立 rts-game 的开发仓库、项目基础文档和技术路线，为后续 Three.js 3D RTS 原型开发准备可追踪的基线。

## References

- [项目背景与范围](../project-background.md)
- [渲染与游戏框架对比](../engine-comparison.md)
- [技术开发方案](../technical-solution.md)
- [开发计划](../development-plan.md)
- [OpenClaw 开发与生产发布脚本说明](https://github.com/oc-workspace/openclaw-workspace/blob/main/openclaw-%E5%BC%80%E5%8F%91%E4%B8%8E%E7%94%9F%E4%BA%A7%E5%8F%91%E5%B8%83%E8%84%9A%E6%9C%AC%E8%AF%B4%E6%明.md)
- [参考进度记录格式](https://github.com/oc-workspace/rococo-outreach/blob/main/docs/progress/2026-08-09.md)

## Completed

### Project initialization

- 通过 ssh rococo-oc-workplace 进入开发服务器。
- 在 /work/oc-projects/rts-game 创建项目目录。
- 使用 oc-create-repo 创建 oc-workspace/rts-game private dev 仓库。
- 初始化 Git、建立 main 分支，并推送到 origin/main。
- 修正引擎对比文档中的 Phaser 官方链接，并通过 oc-push 推送修正。

### Documentation baseline

- 创建 README 与 .gitignore。
- 创建 docs/ 目录。
- 完成项目背景、范围、质量基线和里程碑文档。
- 完成 Three.js、PixiJS、Phaser 3、Phaser 4 的对比和选型结论。
- 确认开发侧与生产桥接侧的职责和发布方式，具体脚本细节不在项目仓库中展示。
- 建立本目录的按日期开发进度记录约定。

### Technical decision

- 3D 战场核心采用 Three.js。
- HUD 第一阶段采用 DOM/CSS 与 Canvas。
- PixiJS 保留为高密度 2D 小地图或 overlay 的可选模块。
- Phaser 3/4 不作为 3D 舰队战场的核心引擎，可用于独立的 2D 原型实验。

### Production repository synchronization

- 在 rococo-root 的 /opt/docker/for-oc-dev2prod/rts-game 建立隔离检出。
- 因生产桥接机默认 GitHub 身份没有 dev 仓库读取权限，使用脚本支持的 DEV_URL 指向已配置的 github-oc-workspace-write 别名完成克隆。
- 创建 oWinnieo/rts-game private prod 仓库。
- 为 prod 仓库配置专用 deploy key 和 github-owinnieo-rts-game SSH host。
- 使用 oc-prod-init 将 dev 的 main 首次推送到 prod 的 main。
- 生产初始化时核验 dev/main、隔离检出 HEAD 和 prod/main 均指向提交 8274760。

### Development solution and plan

- 确认此前只有背景、引擎选型、发布流程和进度记录，没有独立的技术方案与开发计划。
- 新增技术方案，明确 TypeScript/Vite/Three.js、Simulation/Renderer/UI 分层、固定步长、随机 seed、性能策略和验证方式。
- 新增开发计划，拆分 P0 至 P7 里程碑、任务顺序、阶段验收标准、Definition of Done 和风险控制。
- 明确远程 /work/oc-projects/rts-game 是正式 dev 工作区，本地目录仅用于文件准备和同步辅助。
- 更新 docs 索引，保证后续开发可以从文档直接找到方案、计划和进度入口。

### P1 engineering skeleton

- 在远程 dev 工作区初始化 TypeScript、Vite 和 Three.js 工程。
- 创建 3D 星空测试场景、导航 beacon、相机、窗口自适应和 WebGL 错误提示。
- 创建固定步长循环、固定 seed、FPS/Simulation/Active Units HUD。
- 添加 R 重置镜头和 P 暂停控制。
- 远程 npm install 成功，生成依赖 lockfile。
- npm run typecheck 通过。
- npm run build 通过；Vite 提示 Three.js 初始 bundle 超过 500 kB，记录为后续性能优化项。
- 远程开发服务器通过 Node fetch HTTP smoke test，入口页面可正常返回。

### Preview deployment preparation

- 核对 netcup1 上现有项目的 Docker Compose 约定：项目独立 Compose、宿主机端口和共享 Nginx 域名转发。
- 确认 `rts-game-dev.rococo.dev` 已解析到 netcup1，且现有 `rococo.dev` 通配证书可以覆盖该子域名。
- 新增项目级多阶段 Dockerfile：构建阶段执行 `npm ci` 和 `npm run build`，运行阶段由 Nginx 提供 Vite `dist` 产物。
- 预览容器使用宿主机 `3109` 端口，共享 Nginx 转发到 `172.18.0.1:3109`。
- 新增预览部署说明，记录项目容器、共享 Nginx、域名和浏览器验收边界；不保存任何凭据。

### Preview deployment verification

- netcup1 的 `/opt/docker/oc-projects/rts-game` 已使用提交 `3b7a813` 构建并启动 `rts-game-dev-rts-game-1`。
- Docker 多阶段构建成功；容器内再次通过 `npm run typecheck` 和 `npm run build`。
- 共享 Nginx `nginx -t` 通过并已 reload；`rts-game-dev.rococo.dev` 的 HTTP 301、HTTPS 200 和入口 HTML smoke test 均通过。
- 浏览器打开 HTTPS 预览成功，页面标题、HUD、星空、网格和导航 beacon 可见；控制台无 error/warning。
- 浏览器快捷键 `P` 已验证可以暂停并恢复运行。
- 记录已知限制：Three.js 初始 bundle 仍有超过 500 kB 的构建警告，留给后续 P6 性能阶段处理。

### P2 single ship command

- 定义 `ShipClass`、`Unit`、`MoveOrder` 和 `WorldState`，固定步长移动规则不依赖 Three.js Mesh。
- 创建一艘程序化 `AURORA SCOUT` 测试舰，加入阵营色材质、推进光、选择环和移动目标标记。
- 实现左键选择、右键移动、滚轮缩放、中键平移、`P` 暂停、`N` 重开和 `R` 重置相机。
- 增加选中单位状态卡片，显示舰船名称、类型、生命值和当前指令。
- 远程 dev 工作区已通过 `npm run typecheck` 和 `npm run build`；Vite bundle 体积警告继续保留为后续性能项。

### P2 preview verification

- netcup1 已使用提交 `10ae061` 重建 `rts-game-dev-rts-game-1`，容器正常运行。
- HTTPS 页面标题已更新为 `RTS Game // P2 Single Ship Command`，构建产物加载正常，控制台无 error/warning。
- 左键选择测试舰成功，状态卡片显示 `SCOUT-01`、`AURORA SCOUT // LIGHT RECON` 和 `100 / 100`。
- 右键移动成功，状态卡片显示 `MOVE ORDER`，视觉检查确认舰船移动到目标方向。
- `P` 暂停/恢复通过；`N` 重开后清除选择并恢复初始 encounter；固定 seed 仍为 `RTS-P1-001`。
- 记录一份固定视角浏览器视觉检查：星空、战术网格、beacon、测试舰、选中环和单位卡片均可读。

### P3 combat loop

- 定义 `scout`、`striker`、`carrier` 三类 `ShipClass`，加入速度、生命值、射程、伤害和冷却差异。
- 加入 `player` / `enemy` 阵营、目标锁定、右键攻击、`S` 停止、攻击轨迹、命中和销毁效果。
- 加入敌方交战行为、生命值归零后的移除和 `VICTORY` / `DEFEAT` 战斗结果。
- HUD 增加目标、武器冷却、生命值和战斗结果反馈。
- 第一次浏览器验收发现敌方在玩家下令前过早开火；通过 `playerHasEngaged` 和重新平衡生命/伤害/冷却修正，提交 `a4b68cd` 重新部署。

### P3 combat verification

- 服务器已使用 `a4b68cd` 重建预览容器，HTTPS 页面正常加载，控制台无 error/warning。
- 浏览器固定 seed `RTS-P3-001` 载入 3 艘舰船；左键选择 `SCOUT-01`，右键攻击 `STRIKER-01` 成功。
- `STRIKER-01` 被击毁后，继续攻击 `CARRIER-01`；最终 HUD 显示 `VICTORY`、`ACTIVE UNITS 1`，玩家生命值 `48 / 140`。
- 攻击命中、冷却、销毁和胜负状态均可在同一固定 seed 下复现。

### P3.5 visual baseline correction

- 根据线上预览反馈暂停进入 P4，先处理舰船、场景和 HUD 明显偏幼稚、粗糙的问题。
- 新增视觉方向文档，明确采用克制的军事硬科幻语言：深色金属主体、低饱和阵营识别、功能性细节和紧凑战术 HUD。
- 重建 scout、striker、carrier 的程序化轮廓，加入切角舰体、装甲层、武器挂点、侧舱、指挥舱、引擎壳体和小面积识别条。
- 将敌方舰体从容易呈现木块感的暖棕色修正为中性枪灰，仅保留暖红色敌我识别和推进光。
- 重做相机、曝光、主光、轮廓光与填充光；两轮固定视角检查后提高舰体中间调可读性，并略微拉近构图。
- 将巨型高亮导航球改为小型工业 relay，降低网格、信标、选择环、命中特效和背景粒子的视觉权重。
- 收紧标题、状态、单位卡片和底部 telemetry，使用低饱和玻璃/金属样式，避免调试面板占据主要画面。

### P3.5 verification

- dev 提交 `901aee2` 建立第一版视觉基线，`742bf5f` 修正过暗舰体和偏远构图，`9a1195b` 继续收敛敌方材质并细化舰船轮廓。
- 三次服务器构建均通过 `git diff --check`、`npm run typecheck` 和 `npm run build`，netcup1 预览容器均成功重建。
- 最终使用隔离浏览器配置在 1440×900 固定视角检查：三类舰船可区分、舰体层次可读、信标不再抢画面，HUD 完整且不遮挡战场。
- HTTPS 页面标题和运行 telemetry 正常，浏览器控制台无 error/warning；P3 既有战斗逻辑未在本轮修改。
- 已知限制：当前仍是程序化原型资产，不是 AAA 最终模型；缺少高精度纹理/贴花、损伤层、环境资产、阴影和后处理。Three.js 主 bundle 仍有超过 500 kB 的构建警告。

## Current state

- dev 仓库已经创建并推送，远程开发工作区的 main 与 origin/main 保持对齐。
- prod 仓库已经创建并初始化，oWinnieo/rts-game 的 main 与本次同步快照 8274760 对齐。
- 开发侧已新增生产同步记录、技术方案和开发计划；这些开发侧文档后续应通过 oc-prod-release 发布。
- P1 工程骨架、P2 单舰控制、P3 最小战斗闭环和 P3.5 视觉基线均已通过远程构建与服务器部署验收。
- 预览环境的 Docker/Nginx 链路、P2 交互闭环、P3 战斗闭环和 P3.5 固定视角视觉回归均已验证；后续更新继续复用同一域名。
- 生产桥接机上的 oc-prod-clone、oc-prod-create-repo 和 oc-prod-init 已完成；后续更新应使用 oc-prod-release。
- 当前质量状态是“成熟视觉方向已建立的程序化原型”，还不是可玩的 RTS vertical slice，也不是 AAA 最终资产质量。

## Next actions

- 进入 P4：加入 deterministic random 遭遇生成、双方初始舰队和中立物。
- 加入多选、编组、编队锚点、共享指令和随机 seed 展示/复制。
- 扩展 HUD：舰队列表、战斗日志、小地图占位和当前遭遇状态。
- 在 P4 功能稳定后安排专门资产阶段：舰船概念图、PBR 纹理/贴花、损伤层、环境资产和轻量后处理。
- 在 GitHub 上为 prod/main 配置分支保护或 Ruleset。
- 后续生产发布使用 oc-prod-release 创建 release 分支并通过 PR 合并。

## Security notes

- 本次记录未保存 GitHub token、SSH 私钥、密码、授权码或其他敏感信息。
- 生产仓库创建和首次初始化已在生产桥接服务器完成；后续变更仍需使用隔离发布流程，具体细节不在项目仓库中展示。
- 不把“AAA”或与商业游戏的比较写成未经验证的完成结论；后续质量判断必须基于可重复的运行、性能和视觉检查。
