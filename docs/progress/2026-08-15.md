# 2026-08-15 Progress Log

## Scope

推进 P8 的 scout、striker 与 carrier 近景视觉升级，落地舰船资产规格，完成第二轮材质/贴花/损伤实现、桌面视觉签核、远程构建、预览回归和生产仓库分支同步。

## References

- [开发计划](../development-plan.md)
- [P8 舰船视觉资产规格](../p8-visual-asset-spec.md)
- [2026-08-14 进度记录](2026-08-14.md)
- [开发预览部署说明](../preview-deployment.md)

## Completed

### P8 asset spec

- 新增 `docs/p8-visual-asset-spec.md`，把 scout、striker、carrier 的视觉职责、资产层级契约、材质预算和性能预算写成可执行规范。
- 明确正式资产接入时的朝向、挂点、回退资产和贴图/三角面记录要求。
- `docs/README.md` 已补上新资产规格入口。

### Scout first visual pass

- scout 近景补上了更清晰的背脊、传感器罩、传感器条、侧翼装甲、散热片、翼尖标识和传感器桅杆。
- 保留了现有 selection ring、distant LOD、thrusters、damage overlays 和阵营高低特效路径，没有动 simulation 或指令逻辑。

### Striker first visual pass

- striker 近景补上中央龙骨、指挥舱、火控传感器、宽肩武器舱、后坐机构、炮管护套和侧面散热器，强化“宽体、双炮位、前向压制”的轮廓职责。
- 重新抬高中央阵营识别条，避开 registry decal 的原有高度，并让识别条落在新增龙骨上方。
- striker 的 `detailGroup` 在加上 registry、damage overlays 和 thrusters 后共 32 个直接子网格，未超过 P8 规格中的上限。
- 本轮只调整渲染层，保留 selection、movement、combat、distant LOD、instanced batch 和高低特效路径。

### Carrier first visual pass

- carrier 近景补上前后分段装甲、双侧机库眉梁与支撑、外侧装甲轨、散热器、着舰引导灯、两级指挥塔、传感器条和传感器冠，强化“大体量、双侧机库、中央指挥塔”的轮廓职责。
- carrier 的 `detailGroup` 在加上 registry、damage overlays 和三组 thrusters 后共 39 个直接子网格，未超过 P8 规格中的 42 个上限。
- 本轮仍只调整 `src/render/ship-visuals.ts` 的渲染层，没有修改 simulation、舰船数值、指令逻辑、远景实例映射或高低特效规则。
- 新增固定 seed 桌面与手机基线：`assets/p8-carrier-20260815-desktop-1280x720.jpg`、`assets/p8-carrier-20260815-mobile-390x844.jpg`。

### P8 second visual pass

- 在不增加任何子网格的前提下，为三舰种统一了 hull、segmented armor、functional panel 和 opaque glass 四类近景表面语言；sensor canopy、striker command blister 和 carrier bridge 使用克制的阵营反射玻璃材质。
- 新增共享 128×128 装甲、功能面板和面板粗糙度纹理；所有纹理由 Canvas 在运行时生成，不增加外部资源请求，远景实例路径仍使用原有低成本材质。
- registry decal 改为按舰种生成：`SC-01 / RECON`、`ST-02 / LINE`、`CV-03 / COMMAND` 各自带轮廓图标，但继续复用现有单张贴花 plane。
- damage overlays 改为三阶段烧蚀、裂纹和结构破口纹理，并按 scout、striker、carrier 的表面尺寸分别布置；显示阈值为 18%、42% 和 68% 损伤，低特效规则保持不变。
- 共享规格常量移出单舰创建路径，200 单位初始化不会为每艘舰重复创建贴花和损伤位置配置对象。
- 新增本轮实际构建的桌面全景与航母近景基线：`assets/p8-round2-20260815-desktop-1280x720.jpg`、`assets/p8-round2-20260815-carrier-closeup-1280x720.jpg`。

### Verification and deployment

- 加入 striker 首轮视觉升级后，远程 dev 工作区再次通过 `npm test`（2 files / 9 tests）、`npm run typecheck` 和 `npm run build`。
- 构建仍只有既有的 Vite 大 chunk warning，约 609 kB。
- `https://rts-game-dev.rococo.dev/?seed=20260810` 已重新构建部署。
- 内置浏览器 screenshot 本轮可用；桌面 1280×720 下主 canvas 的 CSS 尺寸匹配视口、backing buffer 为 2560×1440，页面无横向或纵向溢出。
- 桌面实景中成功选中 `P-STRIKER-03`，并通过右键下达移动命令；新增宽肩、双炮护套、中央龙骨与散热轮廓可辨认，选择环和移动状态正常。
- 手机 390×844 下主 canvas 匹配视口，HUD 按移动端规则堆叠且 body 无溢出；本轮桌面与手机检查的页面 console logs 均为空。
- carrier 完成后，远程 dev 工作区再次通过 `npm test`（2 files / 9 tests）、`npm run typecheck`、`npm run build` 和线上 HTTP smoke；主 bundle 约 610.61 kB，仍只有既有的大 chunk warning。
- 固定 seed 桌面实景中成功选中并聚焦 `P-CARRIER-04`，下达移动和攻击指令，并验证高低特效切换、生命值、目标、战斗日志、损伤和摧毁路径。
- 200 单位场景成功从远景实例批次选中 `P-CARRIER-47`；选中后 detail 计数从 0 恢复到 1，完整近景 LOD、`instanceId -> unitId` 映射和选择卡片正常。
- 390×844 下主 WebGL canvas 的 CSS 尺寸与 backing buffer 均为 390×844，body/document 无横向或纵向溢出；本轮最终浏览器 console error/warning 为空。
- 第二轮完成后，远程 dev 工作区再次通过 `npm test`（2 files / 9 tests）、`npm run typecheck`、`npm run build` 和线上 HTTP smoke；主 bundle 约 613.34 kB，比第一轮增加约 2.73 kB，仍只有既有的大 chunk warning。
- 桌面实景重新验证 `P-CARRIER-04` 选择、攻击、受伤、目标卡和高/低特效双向切换；200 单位场景仍保持约 34 个空闲 draw calls，并成功从实例批次选中 `P-CARRIER-47`、恢复 1 个完整 detail LOD。
- 390×844 下主 WebGL canvas 的 CSS 与 backing buffer 均为 390×844，body/document 均为 390×844，无滚动溢出；最终页面 console error/warning 为空。
- 本轮后续截图会话恢复了桌面取图，已保存固定 seed 的 1280×720 全景和 `P-CARRIER-04` 近景；目视检查确认分段装甲、深色功能面板、克制的阵营玻璃和 registry 图标可辨认，未发现选择环/武器遮挡、透明排序或明显 z-fighting 回退。
- 390×844 页面交互、canvas 尺寸与无溢出结果仍有效，但第二轮移动端截图接口再次超时并关闭目标页；因此继续保留第一轮手机基线，不把它冒充为第二轮截图，移动端新基线仍未标记完成。

### Production repository sync

- 按 `openclaw-开发与生产发布脚本说明.md` 的隔离发布流程，从 `rococo-root:/opt/docker/for-oc-dev2prod/rts-game` 运行 `oc-prod-release`。
- 当前开发 `main` 通过新分支 `release/2026-08-15-093343` 同步到 `oWinnieo/rts-game`，生产 `main` 不被直接覆盖，仍需通过 GitHub PR 审核合并。
- 既有 PR #3（`release/v0.1.0-rc.1`）保持不变；P8 后续开发不追加到该冻结候选分支。
- `docs/development-and-release.md` 不在开发 `main` 的 Git tree 中，因此不会出现在本次生产同步分支。
- carrier 首轮升级发生在上述生产同步之后，目前只进入开发 `main` 和开发预览，不在 `release/2026-08-15-093343` 中。

## Current state

- P8 的 scout、striker 与 carrier 第一轮视觉轮廓和第二轮程序化材质、舰种贴花、分阶段损伤层已经落地并可在预览环境运行。
- 目前还没有替换成正式 PBR/bitmap 资产；第二轮代码、交互回归和桌面视觉签核已完成，第二轮移动端截图基线仍待补充。
- 现阶段验证重点仍是可读性、稳定性和不破坏 selection/move/attack/LOD/instancing。

## Next actions

- 在移动端截图能力恢复后，仅补交固定 seed 的第二轮 390×844 手机基线；桌面全景与航母近景不需要重复取图。
- 补图后做 P8 最终审计，核对三类舰船近景识别、材质一致性、分阶段损伤位置和低特效可读性，再决定是否形成 `v0.1.0-rc.2`。
- 在 P8 最终审计前不提前进入 P9，也不扩大 P10 内容范围。

## Security notes

- 本次记录未保存 token、SSH 私钥、密码、授权码、邮箱密码或其他敏感信息。
