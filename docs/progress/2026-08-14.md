# 2026-08-14 Progress Log

## Scope

处理 `v0.1.0-rc.1` 预览版首次操作不直观的问题，为随机遭遇增加默认可见的快速上手说明，并完成桌面浏览器交互回归和开发预览部署。

## References

- [开发计划](../development-plan.md)
- [2026-08-13 进度记录](2026-08-13.md)
- [v0.1.0-rc.1 发布说明](../releases/v0.1.0-rc.1.md)
- [开发预览部署说明](../preview-deployment.md)

## Completed

### Quick start guide

- 在首次进入页面时增加 `FIRST CONTACT // 快速上手` 面板，明确说明选择、移动、攻击、追加指令、暂停/停止、控制组和获胜条件。
- 底部快捷提示改为更直白的 `LMB SELECT · RMB MOVE/ATTACK · SHIFT RMB QUEUE · H HELP · S STOP · P PAUSE`。
- 增加 `CLOSE` 和 `HOW TO PLAY` 入口；关闭后可通过按钮或 `H` 重新打开，不需要刷新页面。
- 说明镜头控制：滚轮缩放、中键拖拽平移、`R` 重置相机、小地图点击聚焦。
- 修复快速上手面板在桌面尺寸下的内容布局、关闭按钮越界和重开按钮层级问题。

### Verification and deployment

- 远程 dev 工作区通过 `npm test`（2 个测试文件、9 项通过）、`npm run typecheck` 和 `npm run build`。
- 构建仍只有约 608 kB 主 chunk 的既有 Vite 警告，无新增构建错误。
- `https://rts-game-dev.rococo.dev/?seed=20260810` 已重新构建部署。
- Codex 内置 Chromium/WebGL 桌面视口验证：快速上手面板完整显示在视口内；关闭后 `HOW TO PLAY` 按钮可见且可重新打开；控制台无 error/warning。
- 本次实现提交：`12ef35c`、`9c8a196`、`170acda`、`f91616b`、`cba1a04`；开发 `main` 已推送到最新提交。

## Current state

- 新玩家现在可以在页面内直接看到操作路径，不需要先阅读外部发布说明。
- 当前仍是随机遭遇 vertical slice；默认目标是选择蓝色玩家舰船，右键移动/攻击并消灭红色敌舰。
- 本次可用性改动已进入 `oc-workspace/rts-game` 的 dev `main` 和开发预览；既有 `oWinnieo/rts-game` PR #3 是此前候选版分支，未自动改写生产 PR。

## Next actions

- 收集真实试玩反馈，重点观察玩家是否能完成第一次选舰、移动和攻击。
- 如果确认快速上手流程有效，再开始 P8 舰船资产与视觉质量升级。
- 若要把本次快速上手改动纳入生产候选版，需要基于最新 dev `main` 更新或重新创建隔离 release PR，不直接修改生产 `main`。

## Security notes

- 本次记录未保存 token、SSH 私钥、密码、授权码、邮箱密码或其他敏感信息。
